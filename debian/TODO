= Misc =
 * see current bugs
 * compare installed files with traditional method
 * try to rebuild rdeps
 * move ACE_ROOT from /usr/share/ace to /usr/lib/ace?
 * hide .real programs in /usr/lib/ace?
 * make all TAO programs start with tao-, and put
   distribution compatible program in /usr/lib/tao

= MWC and MPC =
 * they should define $ACE_ROOT and $TAO_ROOT if
   they're not

= autoconf support =
 * some macros are missing (see rules)

= TAO =
 * when ACE_HAS_IPV6 is set and no endpoint is specified, only IPv6 is
   tried

= CIAO =
 * autoconf support
 * cidlc
